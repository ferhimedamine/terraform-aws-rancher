stages:
  - test
terraform apply:
  stage: test
  image: alpine:latest
  script:
    - "echo terraform apply..."
  only:
    refs:
      - /^feat.*$/
    variables:
      - $CI_COMMIT_MESSAGE =~ /#tf_apply/
  except:
    refs:
      - master